<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/payday_auth_background"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:gravity="center"
            android:text="‹"
            android:textColor="@color/payday_text_primary"
            android:textSize="36sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/bg_auth_card"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="20dp"
                android:text="@string/sign_up"
                android:textColor="@color/payday_text_primary"
                android:textSize="24sp"
                android:textStyle="bold" />

            <TextView style="@style/AuthLabel" android:text="@string/email" />
            <EditText android:id="@+id/emailInput" style="@style/AuthInput" android:hint="@string/email_hint" android:inputType="textEmailAddress" />
            <TextView style="@style/AuthLabel" android:layout_marginTop="12dp" android:text="@string/password" />
            <EditText android:id="@+id/passwordInput" style="@style/AuthInput" android:hint="@string/password_rule" android:inputType="textPassword" />
            <TextView style="@style/AuthLabel" android:layout_marginTop="12dp" android:text="@string/password_confirm" />
            <EditText android:id="@+id/passwordConfirmInput" style="@style/AuthInput" android:hint="@string/password_confirm_hint" android:inputType="textPassword" />
            <TextView style="@style/AuthLabel" android:layout_marginTop="12dp" android:text="@string/nickname" />
            <EditText
                android:id="@+id/nicknameInput"
                style="@style/AuthInput"
                android:hint="@string/nickname_hint"
                android:inputType="textPersonName"
                android:maxLength="10" />

            <Button
                android:id="@+id/signUpButton"
                android:layout_width="match_parent"
                android:layout_height="54dp"
                android:layout_marginTop="24dp"
                android:background="@drawable/bg_expense_save"
                android:enabled="false"
                android:text="@string/join"
                android:textAllCaps="false"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:textStyle="bold" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
